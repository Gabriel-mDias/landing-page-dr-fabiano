import Lenis from 'lenis'; import gsap from 'gsap'; import { ScrollTrigger } from 'gsap/ScrollTrigger';
gsap.registerPlugin(ScrollTrigger);
export function initAnimations() { const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches; if (reduced) { document.querySelectorAll('[data-reveal]').forEach((el) => el.classList.add('is-visible')); return null; } const lenis = new Lenis({ duration: 1, smoothWheel: true }); lenis.on('scroll', ScrollTrigger.update); gsap.ticker.add((time) => lenis.raf(time * 1000)); gsap.ticker.lagSmoothing(0); gsap.utils.toArray('[data-reveal]').forEach((el) => gsap.fromTo(el, { opacity: 0, y: 30 }, { opacity: 1, y: 0, duration: 0.8, ease: 'power3.out', scrollTrigger: { trigger: el, start: 'top 88%', once: true } })); const story = document.querySelector('[data-care-story]'); const progress = document.querySelector('[data-story-progress]'); if (story && progress) gsap.to(progress, { scaleX: 1, ease: 'none', scrollTrigger: { trigger: story, start: 'top 25%', end: 'bottom 70%', scrub: true } }); return lenis; }
